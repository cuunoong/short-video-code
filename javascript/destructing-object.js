const user = {
  name: "Arif Iskandar",
  role: "Developer",
};

// const name = user.name;
// const role = user.role;

// console.log(name, role);

const { name, role } = user;
console.log(name, role);
